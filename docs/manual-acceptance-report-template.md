# 最终人工验收记录模板

## 1. 使用边界

本文是 `final-acceptance-checklist.md` 的记录模板，用于最终统一手测时留下可复查证据。它不是任务状态源，也不替代 `TASKS.md`、`AI_HANDOFF.md` 或发布流程。

填写时不要记录 Cookie、session、csrf token、用户源码、完整请求体、本机数据库内容或可复用登录态。站点触发验证码、登录过期、比赛权限或风控时，按“待复测”记录，不用旧数据强行判定通过。

## 2. 基本信息

| 字段 | 记录 |
|---|---|
| 验收日期 |  |
| 验收人 |  |
| 分支/提交 |  |
| 系统环境 | Windows  |
| 应用版本 |  |
| 是否使用安装包 | 是 / 否 |
| 数据目录是否备份 | 是 / 否 / 不适用 |

## 3. 自动验证记录

在 `algo-electron/` 下执行并记录结果：

| 命令 | 结果 | 备注 |
|---|---|---|
| 2026-07-04 当前基线：`npm run test:all` | 通过 | 由结构巩固收口阶段记录；最终验收前建议按当前分支重新运行。 |
| `npm run typecheck` | 通过 / 失败 / 未运行 |  |
| `npm run test:core` | 通过 / 失败 / 未运行 |  |
| `npm run test:ai` | 通过 / 失败 / 未运行 |  |
| `npm run test:architecture` | 通过 / 失败 / 未运行 |  |
| `npm run test:security` | 通过 / 失败 / 未运行 |  |
| `npm run test:docs` | 通过 / 失败 / 未运行 |  |
| `npm run test:packaging` | 通过 / 失败 / 未运行 |  |
| `npm run test:adapters` | 通过 / 失败 / 未运行 |  |
| `npm run test:submissions` | 通过 / 失败 / 未运行 |  |
| `npm run test:db` | 通过 / 失败 / 未运行 |  |
| `npm run test:electron` | 通过 / 失败 / 未运行 |  |
| `npm run test:ui` | 通过 / 失败 / 未运行 |  |
| `npm run test:all` | 通过 / 失败 / 未运行 |  |
| `git diff --check` | 通过 / 失败 / 仅 LF/CRLF 提示 |  |

## 4. 七站提交监测记录

每站至少记录一次正式提交。预期：判题中、自测、样例运行、公开状态行和查看历史记录不提前入库；最终结果只入库一次。

| 站点 | 题目 URL | 登录/验证码 | 提交 ID 或记录 URL | Verdict | 语言 | 是否跳转/刷新 | UI/数据库表现 | 结论 |
|---|---|---|---|---|---|---|---|---|
| Codeforces |  | 正常 / 待复测 |  |  |  |  |  | 通过 / 失败 / 待复测 |
| AcWing |  | 正常 / 待复测 |  |  |  |  |  | 通过 / 失败 / 待复测 |
| Nowcoder |  | 正常 / 待复测 |  |  |  |  |  | 通过 / 失败 / 待复测 |
| VJudge |  | 正常 / 待复测 |  |  |  |  |  | 通过 / 失败 / 待复测 |
| PTA |  | 正常 / 待复测 |  |  |  |  |  | 通过 / 失败 / 待复测 |
| Luogu |  | 正常 / 待复测 |  |  |  |  |  | 通过 / 失败 / 待复测 |
| LeetCode |  | 正常 / 待复测 |  |  |  |  |  | 通过 / 失败 / 待复测 |

专项注意：

- Nowcoder：自测输入框、自测结果、样例测试、调试运行都不能入库。
- VJudge：公开状态列表、非当前用户、旧记录和 pending 最新行不能入库。
- Luogu：不能把分数或其他数值字段当语言。
- LeetCode：判题中不能入库。

## 5. 手动同步与题目详情

| 项目 | 验收点 | 结果 | 备注 |
|---|---|---|---|
| Codeforces API 同步 | 绑定账号后同步最近提交，重复提交不重复写入。 | 通过 / 失败 / 未测 |  |
| 当前页面提交记录同步 | 只写入当前用户或可明确关联记录。 | 通过 / 失败 / 未测 |  |
| 题目详情提交列表 | verdict、语言、耗时、内存、sourceUrl 可读且倒序。 | 通过 / 失败 / 未测 |  |
| 删除题目 | 关联笔记提示、列表/详情/统计无残留异常。 | 通过 / 失败 / 未测 |  |

## 6. 核心页面与交互

| 区域 | 验收点 | 结果 | 备注 |
|---|---|---|---|
| 题目侧栏 | 平台、状态、搜索、分页、展开/收起、点击跳转。 | 通过 / 失败 / 未测 |  |
| 题目详情 | 基本信息、提交记录、笔记入口、删除按钮、浏览器跳转。 | 通过 / 失败 / 未测 |  |
| 笔记弹层 | 新建、改标题、编辑正文、类型切换、图片上传、删除、重开一致。 | 通过 / 失败 / 未测 |  |
| 统计页 | 平台分布饼图和柱图、趋势图、rating 图、AI 建议、错题/未复习列表。 | 通过 / 失败 / 未测 |  |
| 设置页 | 默认首页、CF 账号绑定/同步、实时提交诊断。 | 通过 / 失败 / 未测 |  |
| 站点管理 | 新增、禁用/启用、导入、预览冲突、确认导入、导出 JSON。 | 通过 / 失败 / 未测 |  |
| 用户脚本 | 导入、编辑、启停、删除、打开目录、目标站点匹配、页面注入。 | 通过 / 失败 / 未测 |  |

## 7. 打包产物验收

| 项目 | 结果 | 备注 |
|---|---|---|
| `npm run build:win` 生成 NSIS 安装包 | 通过 / 失败 / 未测 |  |
| 安装后应用名称、图标、开始菜单和卸载入口正常 | 通过 / 失败 / 未测 |  |
| 首次启动默认首页、基础导航、多标签、题库侧栏、设置页和统计页可用 | 通过 / 失败 / 未测 |  |
| 升级安装不丢用户数据 | 通过 / 失败 / 未测 |  |
| 卸载流程符合预期 | 通过 / 失败 / 未测 |  |
| 安装包不包含 tests/tmp/release/.env/本地数据库/Cookie/源码日志 | 通过 / 失败 / 未测 |  |

## 8. 问题与风险

| 编号 | 问题 | 影响范围 | 复现步骤 | 处理结论 |
|---|---|---|---|---|
| 1 |  |  |  | 修复 / 待复测 / 暂缓 |
| 2 |  |  |  | 修复 / 待复测 / 暂缓 |

## 9. 最终结论

| 结论项 | 记录 |
|---|---|
| 自动验证是否全部通过 | 是 / 否 |
| 七站提交监测是否全部通过 | 是 / 否 / 有待复测 |
| 核心页面是否无阻断问题 | 是 / 否 |
| 打包产物是否通过 | 是 / 否 / 未测 |
| 是否发现敏感信息泄漏 | 否 / 是 |
| 是否允许标记 P8-009 完成 | 是 / 否 |
| 是否允许标记 P8-012 完成 | 是 / 否 |
| 总结 |  |
